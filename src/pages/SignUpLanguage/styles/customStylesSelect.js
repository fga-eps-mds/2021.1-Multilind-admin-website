const styles = {
  control: base => ({
    ...base,
    background: '#F5F5F5'
  }),
  singleValue: base => ({
    ...base,
    color: '#060606'
  }),
  input: base => ({
    ...base,
    color: '#060606'
  })
}

export default styles
